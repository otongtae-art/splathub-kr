# Docker SDK + Python 3.11.
# gradio 5.x 는 HfFolder 의존성 제거 + Gradio 5 REST API 지원.
# gradio_client 도 함께 현대화되어 facebook/vggt (Gradio 5) 호출 가능.
gradio>=5.5.0,<6
gradio_client>=1.5.0
fastapi>=0.115
uvicorn>=0.32
python-multipart>=0.0.17

# 배경 제거 파이프라인
rembg==2.0.61
Pillow==10.4.0
onnxruntime==1.19.2

# VGGT pointcloud → Poisson mesh 변환 (CPU only, 5-10초)
# open3d 는 HF Space 용량 큼 (~200MB) 이지만 surface reconstruction 필수
open3d>=0.18.0
trimesh>=4.3.0
numpy>=1.26
