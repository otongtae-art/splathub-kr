gradio==5.5.0
spaces>=0.28

# TripoSR 공식 의존성
omegaconf==2.3.0
Pillow>=10.1.0
einops==0.7.0
transformers==4.35.0
trimesh==4.0.5
rembg
huggingface-hub>=0.26
imageio[ffmpeg]
xatlas==0.0.9
moderngl==5.10.0

# torchmcubes — isosurface 추출용 (TripoSR 필수)
# HF Spaces 이미지에 torch + CUDA 포함돼 있어 wheel build 가능
git+https://github.com/tatsy/torchmcubes.git

# torch/torchvision 은 HF Spaces ZeroGPU 이미지에 기본 포함

numpy<2.0
